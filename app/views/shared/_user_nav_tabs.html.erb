<ul class="nav nav-tabs nav-justified">
  <li class="nav-item">
    <%= link_to "My Post", user_path(@user.id), class: "nav-link #{ 'active' if current_page?(user_path(@user.id)) }" %>
  </li>
  <li class="nav-item">
    <%= link_to "My Comment", comment_user_path(@user.id), class: "nav-link #{ 'active' if current_page?(comment_user_path(@user.id)) }" %>
  </li>

  <li class="nav-item">
    <%= link_to "My Collect", collect_user_path(@user.id), class: "nav-link #{ 'active' if current_page?(collect_user_path(@user.id)) }" %>
  </li>

  <% if current_user == @user %>
  <li class="nav-item">
    <%= link_to "My Draft", draft_user_path(@user.id), class: "nav-link #{ 'active' if current_page?(draft_user_path(@user.id)) }" %>
  </li>
  <% end %>
</ul>
