<%#= link_to "Edit", edit_post_comment_path(post.id), class: "btn btn-info btn-sm" if current_user == post.user %>
<%#= link_to "Delete", post_comment_path(post.id), method: :delete, data: {disable_with: "waiting.."}, class: "btn btn-danger btn-sm" if current_user.role == "Admin" || current_user == post.user %>
