<div class="container">
  <div class="link">
    <%= link_to "Category List", admin_categories_path, class:" btn btn-link" %>
    <%= link_to "User List", admin_users_path, class:" btn btn-link" %>
  </div>
  <h2>User List</h2>
  <hr>
  <table class="table table-dark table-striped table-hover">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col-md-auto">Email</th>
        <th scope="col">Role</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <th><%= user.name %></th>
          <td><%= user.email %></td>
          <td>
            <%= form_tag admin_user_path(user.id), method: :patch, class: "form-inline" do %>
              <%= select_tag :role, options_for_select([["Admin", "Admin"], ["Normal", "Normal"]], user.role), class: "form-control" %>
              <%= submit_tag "Update", class: "btn btn-info" %>

            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>
