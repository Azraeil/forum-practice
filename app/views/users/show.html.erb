<div class="container">
  <br>
  <%= render partial: "shared/user_content" %>

  <% if current_user != @user %>
    <%= link_to "Add Friend", friend_request_user_path(post_object.user), method: :post, class: "btn btn-sm btn-dark" %>
  <% end %>

  <br>
  <%= render partial: "shared/user_nav_tabs" %>

  <br>

  <!-- 文章列表 -->
  <%= render partial: "shared/user_post" %>
</div>
